#LOCAL_PATH := $(call my-dir)

#include $(CLEAR_VARS)
#LOCAL_MODULE    := libbinocle
#LOCAL_STATIC_LIBRARIES := libbinocle
#LOCAL_SRC_FILES := libbinocle.a
#include $(PREBUILT_STATIC_LIBRARY)

include $(call all-subdir-makefiles)
